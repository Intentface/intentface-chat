{
  "name": "intentface",
  "version": "0.1.0",
  "private": true,
  "description": "Registry metadata for the Intentface styled components — deps and source files per component, consumed by the docs' manual copy-paste blocks.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-registry.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
